<h3 class="text-center mt-5"> Open Project </h3>

<% if @projects.any? %>
<ul>
  <% @projects.each do |project| %>
    <li><%= project.name %> </li>
  <% end %>
<% else %>
<h4 class="text-center mt-3">There are no Projects at this time</h4>
<% end %>